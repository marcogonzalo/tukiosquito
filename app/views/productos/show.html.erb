<% if notice then %><p id="notice"><%= notice %></p><% end %>

<h1><%=h @producto.nombre %></h1>
<p><% if usuario_actual then %>  
  <%= button_to "Agregar", :action=>'agregar_al_carro', :controller=>"tienda", :id=>@producto.id %>
<% else %>
  Seleccionar
<% end %></p>
<details>
  <%= image_tag("producto/" + @producto.imagen) unless @producto.imagen.nil? %> 
  <p><%=h @producto.descripcion %></p>
  <p><%= number_to_currency(@producto.precio, :unit=>"Bs", :delimeter=>".", :separator=>",") %>
</details>

<% if admin_actual then %>
      <%= link_to 'Editar', edit_producto_path(@producto) %> | 
      <%= link_to 'Eliminar', producto, confirm: '¿Borrar definitivamente?', method: :delete %>
<% end %>
